name: Deploy Infra + DB Bootstrap

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options: [dev, staging, prod]

permissions:
  contents: read

concurrency:
  group: terraform-${{ inputs.environment }}
  cancel-in-progress: false

jobs:
  terraform_apply:
    name: Terraform Plan + Apply (guardrails)
    runs-on: self-hosted
    environment: ${{ inputs.environment }}

    env:
      TF_IN_AUTOMATION: "true"

    defaults:
      run:
        shell: bash
        working-directory: environments/${{ inputs.environment }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          clean: true

      - name: Force clean workspace (self-hosted safety)
        shell: bash
        run: |
          set -euo pipefail
          # En self-hosted puede quedar basura entre runs.
          # Esto borra TODO lo no trackeado (incluye .tf/.tfvars “fantasma”).
          git reset --hard
          git clean -ffdx

      - name: Prepare Terraform temp + plugin cache (avoid /tmp issues)
        run: |
          set -euo pipefail
          mkdir -p "${RUNNER_TEMP}/tf-tmp" "${RUNNER_TEMP}/terraform-plugin-cache"
          chmod 700 "${RUNNER_TEMP}/tf-tmp" "${RUNNER_TEMP}/terraform-plugin-cache"
          echo "TMPDIR=${RUNNER_TEMP}/tf-tmp" >> "$GITHUB_ENV"
          echo "TF_PLUGIN_CACHE_DIR=${RUNNER_TEMP}/terraform-plugin-cache" >> "$GITHUB_ENV"

      - name: Ensure tools (jq)
        run: |
          set -euo pipefail
          if ! command -v jq >/dev/null 2>&1; then
            if command -v yum >/dev/null 2>&1; then
              sudo yum -y install jq
            elif command -v apt-get >/dev/null 2>&1; then
              sudo apt-get update -y
              sudo apt-get install -y jq
            fi
          fi
          command -v jq >/dev/null 2>&1 || { echo "jq is required"; exit 1; }

      - name: Show ASG capacity from checked-out code (debug)
        run: |
          set -euo pipefail
          echo "Repo SHA:"
          git rev-parse HEAD
          echo
          echo "Listing *.tf and *.tfvars in environments/${{ inputs.environment }}:"
          ls -la
          echo
          echo "Searching min_size / desired_capacity in this environment:"
          grep -nE "min_size\s*=|desired_capacity\s*=" -n *.tf || true
          echo
          echo "Context around module autoscaling (if present):"
          awk '
            /module "autoscaling"/ {show=1}
            show {print}
            show && /^\}/ {exit}
          ' main.tf || true

      - name: Terraform Init
        run: |
          set -euo pipefail
          rm -rf .terraform .terraform.lock.hcl || true
          terraform init -input=false -upgrade

      - name: Terraform Validate
        run: |
          set -euo pipefail
          terraform validate

      - name: Terraform Plan
        run: |
          set -euo pipefail
          terraform plan -out=tfplan -input=false -lock-timeout=10m
          terraform show -json tfplan > tfplan.json

      - name: Guardrail (prevent ASG scale-to-zero)
        run: |
          set -euo pipefail

          # Bloquea SOLO si el plan BAJA un ASG a 0:
          # before > 0 AND after == 0 (min_size o desired_capacity)
          if jq -e '
            .resource_changes[]
            | select(.type == "aws_autoscaling_group")
            | select(.change.before != null and .change.after != null)
            | .change as $c
            | (
                (
                  ($c.before.min_size? // 0) > 0 and ($c.after.min_size? // 0) == 0
                )
                or
                (
                  ($c.before.desired_capacity? // 0) > 0 and ($c.after.desired_capacity? // 0) == 0
                )
              )
          ' tfplan.json > /dev/null; then
            echo "Guardrail triggered: plan would scale ASG DOWN to 0."
            echo "This should NOT happen if your code sets min_size/desired_capacity to >0."
            echo "Likely cause on self-hosted: leftover untracked *.tf/*.tfvars in the workspace."
            echo "We forced git clean -ffdx; if it still happens, inspect logs above (ASG snippet + file listing)."
            exit 1
          fi

          echo "Guardrail passed."

      - name: Terraform Apply (from plan)
        run: |
          set -euo pipefail
          terraform apply -auto-approve -input=false -lock-timeout=10m tfplan

  wait_for_instance:
    name: Wait for EC2 (SSM target)
    needs: terraform_apply
    runs-on: self-hosted
    environment: ${{ inputs.environment }}

    steps:
      - name: Wait until an instance is running (by Name tag)
        shell: bash
        env:
          SSM_TARGET_TAG_NAME: ${{ secrets.SSM_TARGET_TAG_NAME }}
        run: |
          set -euo pipefail
          [[ -n "${SSM_TARGET_TAG_NAME:-}" ]] || { echo "SSM_TARGET_TAG_NAME missing (Environment secret)"; exit 1; }

          echo "Waiting for a running instance with Name=${SSM_TARGET_TAG_NAME} ..."
          for i in {1..60}; do
            INSTANCE_ID=$(aws ec2 describe-instances \
              --filters "Name=tag:Name,Values=${SSM_TARGET_TAG_NAME}" \
                        "Name=instance-state-name,Values=running" \
              --query "Reservations[].Instances[].InstanceId | [0]" \
              --output text)

            if [[ -n "$INSTANCE_ID" && "$INSTANCE_ID" != "None" ]]; then
              echo "Found running instance: $INSTANCE_ID"
              exit 0
            fi

            sleep 10
          done

          echo "Timed out waiting for running instance with Name=${SSM_TARGET_TAG_NAME}."
          exit 1

  db_bootstrap:
    name: DB Bootstrap
    needs: wait_for_instance
    uses: ./.github/workflows/db-bootstrap.yml
    with:
      environment: ${{ inputs.environment }}
    secrets: inherit
